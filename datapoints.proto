syntax = "proto3";

option go_package = ".;datapoints";

package datapoints;

service DatapointsService {
    rpc Subscribe (SubscriptionRequest) returns (SubscriptionResponse);
}

message SubscriptionRequest {
    repeated string subscriptionIDs = 1;
}

message SubscriptionResponse {
    repeated string successfullySubscribedIDs = 1;
}
